extends Node2D
## Boot scene. CP 0.2: boots straight into the Game shell (instanced child).
## Replaced by the Game state machine in Phase 2 (CP 2.1).

func _ready() -> void:
	print("Geometry Tubbu — boot OK")
